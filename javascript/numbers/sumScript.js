let input = prompt("Enter the numbers to be added together");
let arr = input.split(" ");

let sum = 0;
for (number of arr) {
    sum += Number(number);
}

let result = document.getElementById('sum');
result.textContent = sum;
